# Elastic：主营搜索、日志分析与可观测性，代表产品包括 Elasticsearch、Kibana 和 Logstash。
- name: Elasticsearch
  matchers:
    - part: body
      type: word
      words:
        - "You Know, for Search"

- name: Kibana
  matchers:
    - part: header
      type: word
      words:
        - "kbn-name"
        - "kibana"
    - part: headers.x-app-name
      words:
        - kibana
      type: regex
    - name: version
      part: body
      regex:
        - <kbn-injected-metadata data="{&quot;version&quot;:&quot;([0-9\.]+)
      type: regex
    - group: 1
      name: version
      part: headers.kbn-version
      regex:
        - ^([\d.]+)$
      type: regex

- name: logstash
  matchers:
    - part: body
      type: word
      words:
        - 'logstash'
